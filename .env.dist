# Dotfiles Configuration Defaults
# Copy this file to .env and customize as needed.
# Values here serve as defaults when not set in .env

# Profile Selection
# Comma-separated list of profiles to activate
# Syntax: profile1,profile2 or all,-excluded or -excluded1,-excluded2
# Example: common,work or all,-personal
# Default "all" starts the config UI in exclusion mode (exclude what you don't want)
DOTFILES_PROFILES=all

# Symlink Settings
# Set to "1" to disable creating ~/.local/bin/dotfiles symlink
# DOTFILES_NO_SYMLINK=1
